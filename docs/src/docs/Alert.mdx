import { Alert, Card, CardContent, Stack } from '@azodik/ui';
import { PropsTable, CodeBlock, CodeBlockWithPreview } from '@/components/docs';
import { useLanguageTranslation } from '@/hooks/useLanguageTranslation';

export const AlertHeader = () => {
  const { t } = useLanguageTranslation();
  return <h1>{t('alertComponent')}</h1>;
};

export const AlertDescription = () => {
  const { t } = useLanguageTranslation();
  return <p>{t('alertDescription')}</p>;
};

export const AlertIntroduction = () => {
  const { t } = useLanguageTranslation();
  return (
    <>
      <h2>{t('alertIntroduction')}</h2>
      <p>{t('alertIntroDesc')}</p>
      <ul>
        <li>{t('alertMainWrapper')}</li>
        <li>{t('alertVariants')}</li>
        <li>{t('alertActions')}</li>
        <li>{t('alertCustomization')}</li>
        <li>{t('alertIcons')}</li>
        <li>{t('alertAccessibility')}</li>
      </ul>
    </>
  );
};

export const WhenToUseSection = () => {
  const { t } = useLanguageTranslation();
  return (
    <>
      <h3>{t('whenToUse')}</h3>
      <ul>
        <li>{t('displaySuccessWarning')}</li>
        <li>{t('showImportantNotifications')}</li>
        <li>{t('provideFeedback')}</li>
      </ul>
    </>
  );
};

export const UsageSection = () => {
  const { t } = useLanguageTranslation();
  return <h2>{t('usage')}</h2>;
};

export const VariantsSection = () => {
  const { t } = useLanguageTranslation();
  return <h2>{t('variants')}</h2>;
};

export const AlertVariantsTitle = () => {
  const { t } = useLanguageTranslation();
  return t('alertVariantsTitle');
};

export const SomeVariantsExample = () => {
  const { t } = useLanguageTranslation();
  return <h2>{t('someVariantsExample')}</h2>;
};

export const AllAlertVariantsTitle = () => {
  const { t } = useLanguageTranslation();
  return t('allAlertVariants');
};

export const WithCloseButtonSection = () => {
  const { t } = useLanguageTranslation();
  return <h2>{t('withCloseButton')}</h2>;
};

export const DismissibleAlertTitle = () => {
  const { t } = useLanguageTranslation();
  return t('dismissibleAlert');
};

export const CustomSizingSection = () => {
  const { t } = useLanguageTranslation();
  return <h2>{t('customSizing')}</h2>;
};

export const CustomSizingExampleTitle = () => {
  const { t } = useLanguageTranslation();
  return t('customSizingExample');
};

export const PropsSection = () => {
  const { t } = useLanguageTranslation();
  return <h2>{t('props')}</h2>;
};

export const AccessibilitySection = () => {
  const { t } = useLanguageTranslation();
  return <h2>{t('accessibility')}</h2>;
};

export const DesignGuidelinesSection = () => {
  const { t } = useLanguageTranslation();
  return <h2>{t('designGuidelines')}</h2>;
};

export const AccessibilityList = () => {
  const { t } = useLanguageTranslation();
  return (
    <ul>
      <li>{t('alertsHaveAria')}</li>
      <li>{t('screenReadersAnnounce')}</li>
      <li>{t('focusManagementHandled')}</li>
      <li>{t('colorNotOnlyWay')}</li>
    </ul>
  );
};

export const DesignGuidelinesList = () => {
  const { t } = useLanguageTranslation();
  return (
    <ul>
      <li>{t('useSuccessAlerts')}</li>
      <li>{t('useWarningAlerts')}</li>
      <li>{t('useErrorAlerts')}</li>
      <li>{t('useInfoAlerts')}</li>
      <li>{t('keepAlertMessagesConcise')}</li>
      <li>{t('provideClearCloseActions')}</li>
    </ul>
  );
};

<AlertHeader />

<AlertDescription />

<AlertIntroduction />

<WhenToUseSection />

<UsageSection />

<CodeBlock language="tsx">
{`import { Alert } from '@azodik/ui';`}
</CodeBlock>

<VariantsSection />

<CodeBlockWithPreview 
  title={<AlertVariantsTitle />}
  preview={
    <div className="space-y-4">
      <Alert variant="soft" color="grass" title="Success!">
        Your changes have been saved successfully.
      </Alert>
      <Alert variant="soft" color="ruby" title="Error!">
        Something went wrong. Please try again.
      </Alert>
      <Alert variant="soft" color="amber" title="Warning!">
        Please check your input before proceeding.
      </Alert>
      <Alert variant="soft" color="indigo" title="Info">
        Here's some helpful information for you.
      </Alert>
    </div>
  }
  code={`import { Alert } from '@azodik/ui';

export default function AlertVariants() {
  return (
    <div className="space-y-4">
      <Alert variant="soft" color="grass" title="Success!">
        Your changes have been saved successfully.
      </Alert>
      <Alert variant="soft" color="ruby" title="Error!">
        Something went wrong. Please try again.
      </Alert>
      <Alert variant="soft" color="amber" title="Warning!">
        Please check your input before proceeding.
      </Alert>
      <Alert variant="soft" color="indigo" title="Info">
        Here's some helpful information for you.
      </Alert>
    </Stack>
  );
}`}
/>

<SomeVariantsExample />

<CodeBlockWithPreview 
  title={<AllAlertVariantsTitle />}
  preview={
    <Stack gap={16}>
      <Alert variant="soft" color="ruby" title="Error">
        Something went wrong. Please try again.
      </Alert>
      <Alert variant="soft" color="indigo" title="Info">
        Here's some helpful information for you.
      </Alert>
    </Stack>
  }
>
{`import { Alert, Stack } from '@azodik/ui';

export default function AlertExamples() {
  return (
    <Stack gap={16}>
      <Alert variant="soft" color="ruby" title="Error">
        Something went wrong. Please try again.
      </Alert>
      <Alert variant="soft" color="indigo" title="Info">
        Here's some helpful information for you.
      </Alert>
    </Stack>
  );
}`}
</CodeBlockWithPreview>

<WithCloseButtonSection />

<CodeBlockWithPreview 
  title={<DismissibleAlertTitle />}
  preview={
    <Alert 
      variant="soft" color="amber" 
      title="Warning" 
      onClose={() => console.log('Alert closed')}
    >
      This alert can be dismissed.
    </Alert>
  }
>
{`import { Alert } from '@azodik/ui';

export default function DismissibleAlert() {
  const handleClose = () => {
    console.log('Alert closed');
  };

  return (
    <Alert 
      variant="soft" color="amber" 
      title="Warning" 
      onClose={handleClose}
    >
      This alert can be dismissed.
    </Alert>
  );
}`}
</CodeBlockWithPreview>

<CustomSizingSection />

<CodeBlockWithPreview 
  title={<CustomSizingExampleTitle />}
  preview={
    <Stack gap={16}>
      {/* String values */}
      <Alert 
        variant="soft" color="indigo" 
        title="Custom Padding" 
        padding="10px"
      >
        Custom padding with string value.
      </Alert>
      
      {/* Number values (converted to pixels) */}
      <Alert 
        variant="soft" color="grass" 
        title="Numeric Values" 
        padding={16} 
        width="70%" 
        height="100%"
      >
        Numeric values are automatically converted to pixels.
      </Alert>
      
      {/* All custom properties */}
      <Alert 
        variant="soft" color="amber" 
        title="All Custom" 
        padding="12px 20px" 
        width="500px" 
        height="100%"
      >
        You can use any valid CSS value for padding, width, and height.
      </Alert>
    </Stack>
  }
>
{`import { Alert, Stack } from '@azodik/ui';

export default function CustomSizedAlert() {
  return (
    <Stack gap={16}>
      {/* String values */}
      <Alert 
        variant="soft" color="indigo" 
        title="Custom Padding" 
        padding="20px"
      >
        Custom padding with string value.
      </Alert>
      
      {/* Number values (converted to pixels) */}
      <Alert 
        variant="soft" color="grass" 
        title="Numeric Values" 
        padding={16} 
        width="50%" 
        height="100%"
      >
        Numeric values are automatically converted to pixels.
      </Alert>
      
      {/* All custom properties */}
      <Alert 
        variant="soft" color="amber" 
        title="All Custom" 
        padding="12px 20px" 
        width="500px" 
        height="100px"
      >
        You can use any valid CSS value for padding, width, and height.
      </Alert>
    </Stack>
  );
}`}
</CodeBlockWithPreview>

<PropsSection />

<PropsTable props={[
  {
    name: 'variant',
    type: '"soft" | "surface" | "outline"',
    required: false,
    default: '"soft"',
    description: 'The visual style variant of the alert'
  },
  {
    name: 'size',
    type: '"1" | "2" | "3"',
    required: false,
    default: '"2"',
    description: 'The size of the alert'
  },
  {
    name: 'color',
    type: 'ThemeAccentColor',
    required: false,
    default: 'indigo',
    description: 'The accent color of the alert'
  },
  {
    name: 'radius',
    type: 'ThemeRadius',
    required: false,
    default: 'inherit',
    description: 'The border radius of the alert'
  },
  {
    name: 'title',
    type: 'string',
    required: false,
    default: '-',
    description: 'The title text displayed at the top of the alert'
  },
  {
    name: 'children',
    type: 'React.ReactNode',
    required: true,
    default: '-',
    description: 'The main content of the alert'
  },
  {
    name: 'icon',
    type: 'React.ReactNode',
    required: false,
    default: 'Auto-generated based on variant',
    description: 'Custom icon for the alert. If not provided, uses default icon based on variant'
  },
  {
    name: 'highContrast',
    type: 'boolean',
    required: false,
    default: 'false',
    description: 'Whether to use high contrast colors'
  },
  {
    name: 'className',
    type: 'string',
    required: false,
    default: '""',
    description: 'Additional CSS classes to apply to the alert'
  }
]} />

<AccessibilitySection />

<AccessibilityList />

<DesignGuidelinesSection />

<DesignGuidelinesList />
