import { Select, Card, CardContent } from '@azodik/ui';
import { PropsTable, CodeBlock, CodeBlockWithPreview } from '@/components/docs';
import { useState } from 'react';

export const ControlledSelectExample = () => {
  const [selectedCountry, setSelectedCountry] = useState('');

  const handleChange = (value) => {
    setSelectedCountry(value);
  };

  const options = [
    { value: '', label: 'Select a country' },
    { value: 'us', label: 'United States' },
    { value: 'uk', label: 'United Kingdom' },
    { value: 'ca', label: 'Canada' },
    { value: 'au', label: 'Australia' }
  ];

  return (
    <div>
      <Select
        label="Country"
        value={selectedCountry}
        onChange={handleChange}
        options={options}
      />
      <p className="text-sm mt-1" style={{ color: "var(--color-text-secondary)" }}>
        Selected: {selectedCountry || 'None'}
      </p>
    </div>
  );
};

# Select Component

The Select component provides dropdown selection menus for choosing from predefined options. It's perfect for forms that require users to select from a list of choices.

## Introduction

The Select component includes several complementary features to handle various dropdown selection scenarios:

- Select: the main wrapper for creating dropdown selection menus with predefined options and clean interfaces.
- Select Options: flexible option management with value/label pairs, grouping, and dynamic option loading.
- Select Validation: built-in validation support with error messages, success states, and form integration.
- Select Accessibility: proper ARIA attributes, keyboard navigation, and screen reader support for dropdown controls.
- Select Customization: flexible sizing, styling, and appearance options for different design contexts.
- Select States: support for disabled, loading, and interactive states with appropriate visual feedback.

### When to use
- Choosing from a list of predefined options
- Form fields with limited choices
- Country, state, or category selection
- Settings and configuration options
- Filtering and sorting options

## Usage

<CodeBlock language="tsx">
{`import { Select } from '@azodik/ui';`}
</CodeBlock>

## Variants

<CodeBlockWithPreview 
  title="Basic Select"
  preview={<ControlledSelectExample />}
>
{`import { Select } from '@azodik/ui';
import { useState } from 'react';

export default function BasicSelect() {
  const [selectedCountry, setSelectedCountry] = useState('');

  const handleChange = (value) => {
    setSelectedCountry(value);
  };

  const options = [
    { value: '', label: 'Select a country' },
    { value: 'us', label: 'United States' },
    { value: 'uk', label: 'United Kingdom' },
    { value: 'ca', label: 'Canada' },
    { value: 'au', label: 'Australia' }
  ];

  return (
    <div>
      <Select
        label="Country"
        value={selectedCountry}
        onChange={handleChange}
        options={options}
      />
      <p className="text-sm mt-1" style={{ color: "var(--color-text-secondary)" }}>
        Selected: {selectedCountry || 'None'}
      </p>
    </div>
  );
}`}
</CodeBlockWithPreview>

## Props

<PropsTable props={[
  {
    name: 'options',
    type: '{ value: string; label: string }[]',
    required: true,
    default: '-',
    description: 'Array of options for the select dropdown'
  },
  {
    name: 'label',
    type: 'string',
    required: false,
    default: '-',
    description: 'Label text for the select field'
  },
  {
    name: 'help',
    type: 'string',
    required: false,
    default: '-',
    description: 'Help text to display below the select'
  },
  {
    name: 'error',
    type: 'string',
    required: false,
    default: '-',
    description: 'Error message to display below the select'
  },
  {
    name: 'status',
    type: '"success" | "error"',
    required: false,
    default: '-',
    description: 'Visual status of the select (success or error)'
  },
  {
    name: 'value',
    type: 'string',
    required: false,
    default: '""',
    description: 'Selected value of the select'
  },
  {
    name: 'onChange',
    type: '(value: string) => void',
    required: false,
    default: '-',
    description: 'Callback function called when selection changes'
  },
  {
    name: 'placeholder',
    type: 'string',
    required: false,
    default: '"Select an option"',
    description: 'Placeholder text for the select'
  },
  {
    name: 'disabled',
    type: 'boolean',
    required: false,
    default: 'false',
    description: 'Whether the select is disabled'
  },
  {
    name: 'className',
    type: 'string',
    required: false,
    default: '""',
    description: 'Additional CSS classes to apply'
  }
]} />

## Accessibility

- Selects have proper labels and associations
- Error messages are announced to screen readers
- Keyboard navigation is supported
- Focus management is handled correctly
- Color contrast meets WCAG guidelines

## Design Guidelines

- Always provide clear labels
- Use descriptive option text
- Group related options together
- Provide a default placeholder option
- Ensure sufficient touch targets on mobile
