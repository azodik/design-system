import { 
  Sidebar, 
  SidebarHeader, 
  SidebarContent, 
  SidebarFooter, 
  SidebarMainContent,
  SidebarBrand,
  SidebarUserDropdown,
  SidebarMenu,
  SidebarMenuItem,
  SidebarMenuButton,
  Card, 
  CardContent 
} from '@azodik/ui';
import { PropsTable, CodeBlock, CodeBlockWithPreview } from '@/components/docs';
import { BasicSidebarExample } from '@/components/sidebar-variants/SidebarBasic';
import { SidebarWithIconsExample } from '@/components/sidebar-variants/SidebarWithIcons';
import { SidebarCollapsibleExample } from '@/components/sidebar-variants/SidebarCollapsible';
import { SidebarWithUserProfileExample } from '@/components/sidebar-variants/SidebarWithUserProfile';
import { SidebarWithFooterExample } from '@/components/sidebar-variants/SidebarWithFooter';
import { SidebarResponsiveExample } from '@/components/sidebar-variants/SidebarResponsive';
import SidebarCodePreview from '@/components/docs/SidebarCodePreview';

# Sidebar Component

The Sidebar component provides vertical navigation panels for app layouts and navigation. It's perfect for creating consistent navigation experiences in web applications.

## Introduction

The Sidebar component includes several complementary features to handle various vertical navigation and layout scenarios:

- Sidebar: the main wrapper for creating vertical navigation panels with consistent access to application sections.
- Sidebar Structure: organized layout with header, content, and footer sections for comprehensive navigation management.
- Sidebar Navigation: flexible menu system with items, icons, badges, and active state management for different sections.
- Sidebar Responsiveness: mobile-friendly navigation with collapsible menus and touch-optimized interactions.
- Sidebar Customization: flexible width, colors, styling, and user profile integration for different design needs.
- Sidebar Accessibility: proper ARIA attributes, keyboard navigation, and screen reader support for navigation panels.

### When to use
- Main application navigation
- Dashboard layouts
- Admin interfaces
- Multi-section applications
- Desktop-focused applications

## Usage

<CodeBlock language="tsx">
{`import { 
  Sidebar, 
  SidebarHeader, 
  SidebarContent, 
  SidebarFooter, 
  SidebarMainContent,
  SidebarBrand,
  SidebarUserDropdown,
  SidebarMenu,
  SidebarMenuItem,
  SidebarMenuButton
} from '@azodik/ui';`}
</CodeBlock>

## Basic Example

<SidebarCodePreview
  title="Basic Sidebar"
  preview={<BasicSidebarExample />}
  code={`import { 
  Sidebar, 
  SidebarHeader, 
  SidebarContent, 
  SidebarBrand,
  SidebarMenu,
  SidebarMenuItem,
  SidebarMenuButton,
  SidebarMainContent
} from '@azodik/ui';

function BasicSidebarExample() {
  return (
    <div style={{ display: 'flex', height: '100vh' }}>
      <Sidebar 
        width={isSmallScreen ? 280 : 250}
        isSidebarOpen={isSidebarOpen}
        onSidebarToggle={handleSidebarToggle}
        isSmallScreen={isSmallScreen}
        collapsed={false}
      >
        <SidebarHeader>
          <SidebarBrand>Basic Sidebar</SidebarBrand>
        </SidebarHeader>
        <SidebarContent>
          <SidebarMenu>
            <SidebarMenuItem>
              <SidebarMenuButton active>Dashboard</SidebarMenuButton>
            </SidebarMenuItem>
            <SidebarMenuItem>
              <SidebarMenuButton>Users</SidebarMenuButton>
            </SidebarMenuItem>
            <SidebarMenuItem>
              <SidebarMenuButton>Settings</SidebarMenuButton>
            </SidebarMenuItem>
          </SidebarMenu>
        </SidebarContent>
      </Sidebar>
      
      <SidebarMainContent
        onSidebarToggle={handleSidebarToggle}
        isSidebarCollapsed={false}
        showToggleOnDesktop={false}
        isSmallScreen={isSmallScreen}
      >
        <div className="p-6">
          <h1>Main Content</h1>
          <p>Your application content goes here.</p>
        </div>
      </SidebarMainContent>
    </div>
  );
}`}
/>

## Sidebar with Icons

<SidebarCodePreview
  title="Sidebar with Icons"
  preview={<SidebarWithIconsExample />}
  code={`import { 
  Sidebar, 
  SidebarHeader, 
  SidebarContent, 
  SidebarBrand,
  SidebarMenu,
  SidebarMenuItem,
  SidebarMenuButton,
  SidebarMainContent
} from '@azodik/ui';

function SidebarWithIconsExample() {
  return (
    <div style={{ display: 'flex', height: '100vh' }}>
      <Sidebar width={250}>
        <SidebarHeader>
          <SidebarBrand>Dashboard</SidebarBrand>
        </SidebarHeader>
        <SidebarContent>
         <SidebarMenu>
            <SidebarMenuItem>
              <SidebarMenuButton 
                active
                icon={<BarChartIcon size={16} />}
              >
                Dashboard
              </SidebarMenuButton>
            </SidebarMenuItem>
            <SidebarMenuItem>
              <SidebarMenuButton 
                icon={<UserIcon size={16} />}
              >
                Users
              </SidebarMenuButton>
            </SidebarMenuItem>
            <SidebarMenuItem>
              <SidebarMenuButton 
                icon={<MailIcon size={16} />}
              >
                Messages
              </SidebarMenuButton>
            </SidebarMenuItem>
            <SidebarMenuItem>
              <SidebarMenuButton 
                icon={<SettingsIcon size={16} />}
              >
                Settings
              </SidebarMenuButton>
            </SidebarMenuItem>
          </SidebarMenu>
        </SidebarContent>
      </Sidebar>
      
      <SidebarMainContent>
        <div className="p-6">
          <h1>Dashboard</h1>
          <p>Welcome to your dashboard!</p>
        </div>
      </SidebarMainContent>
    </div>
  );
}`}
/>

## Collapsible Sidebar

<SidebarCodePreview
  title="Collapsible Sidebar"
  preview={<SidebarCollapsibleExample />}
  code={`import { useState } from 'react';
import { 
  Sidebar, 
  SidebarHeader, 
  SidebarContent, 
  SidebarBrand,
  SidebarMenu,
  SidebarMenuItem,
  SidebarMenuButton,
  SidebarMainContent
} from '@azodik/ui';

function CollapsibleSidebarExample() {
  const [isCollapsed, setIsCollapsed] = useState(false);

  return (
    <div style={{ display: 'flex', height: '100vh' }}>
      <Sidebar 
        width={isCollapsed ? 60 : 250}
        collapsed={isCollapsed}
      >
        <SidebarHeader>
          <SidebarBrand>My App</SidebarBrand>
        </SidebarHeader>
        <SidebarContent>
          <SidebarMenu>
            <SidebarMenuItem>
              <SidebarMenuButton 
                active
                icon={<HomeIcon size={16} />}
              >
                Home
              </SidebarMenuButton>
            </SidebarMenuItem>
            <SidebarMenuItem>
              <SidebarMenuButton 
                icon={<BarChartIcon size={16} />}
              >
                Analytics
              </SidebarMenuButton>
            </SidebarMenuItem>
            <SidebarMenuItem>
              <SidebarMenuButton 
                icon={<UserIcon size={16} />}
              >
                Team
              </SidebarMenuButton>
            </SidebarMenuItem>
          </SidebarMenu>
        </SidebarContent>
      </Sidebar>
      
      <SidebarMainContent
        onSidebarToggle={handleSidebarToggle}
        isSidebarCollapsed={isSmallScreen ? false : isCollapsed}
        showToggleOnDesktop={true}
      >
        <div className="p-6">
          <h1>Main Content</h1>
          <p>Click the toggle button to collapse/expand the sidebar.</p>
        </div>
      </SidebarMainContent>
    </div>
  );
}`}
/>

## Sidebar with User Profile

<SidebarCodePreview
  title="Sidebar with User Profile"
  preview={<SidebarWithUserProfileExample />}
  code={`import { 
  Sidebar, 
  SidebarHeader, 
  SidebarContent, 
  SidebarFooter,
  SidebarBrand,
  SidebarMenu,
  SidebarMenuItem,
  SidebarMenuButton,
  SidebarUserDropdown,
  SidebarMainContent
} from '@azodik/ui';

function SidebarWithUserExample() {
  const userMenuItems = [
    { label: 'Profile', onClick: () => console.log('Profile clicked') },
    { label: 'Settings', onClick: () => console.log('Settings clicked') },
    { divider: true },
    { label: 'Sign Out', onClick: () => console.log('Sign out clicked') }
  ];

  return (
    <div style={{ display: 'flex', height: '100vh' }}>
      <Sidebar width={250}>
        <SidebarHeader>
          <SidebarBrand>Admin Panel</SidebarBrand>
        </SidebarHeader>
        <SidebarContent>
          <SidebarMenu>
            <SidebarMenuItem>
              <SidebarMenuButton active>Dashboard</SidebarMenuButton>
            </SidebarMenuItem>
            <SidebarMenuItem>
              <SidebarMenuButton>Users</SidebarMenuButton>
            </SidebarMenuItem>
            <SidebarMenuItem>
              <SidebarMenuButton>Reports</SidebarMenuButton>
            </SidebarMenuItem>
            <SidebarMenuItem>
              <SidebarMenuButton>Settings</SidebarMenuButton>
            </SidebarMenuItem>
          </SidebarMenu>
        </SidebarContent>
        <SidebarFooter>
          <SidebarUserDropdown
            name="John Doe"
            email="john@example.com"
            menuItems={userMenuItems}
          />
        </SidebarFooter>
      </Sidebar>
      
      <SidebarMainContent>
        <div className="p-6">
          <h1>Admin Dashboard</h1>
          <p>Welcome back, John!</p>
        </div>
      </SidebarMainContent>
    </div>
  );
}`}
/>

## Sidebar with Footer

<SidebarCodePreview
  title="Sidebar with Footer"
  preview={<SidebarWithFooterExample />}
  code={`import { 
  Sidebar, 
  SidebarHeader, 
  SidebarContent, 
  SidebarFooter,
  SidebarBrand,
  SidebarMenu,
  SidebarMenuItem,
  SidebarMenuButton,
  SidebarMainContent
} from '@azodik/ui';

function SidebarWithFooterExample() {
  return (
    <div style={{ display: 'flex', height: '100vh' }}>
      <Sidebar width={250} showFooter={true}>
        <SidebarHeader>
          <SidebarBrand>My App</SidebarBrand>
        </SidebarHeader>
        <SidebarContent>
          <SidebarMenu>
            <SidebarMenuItem>
              <SidebarMenuButton active>Dashboard</SidebarMenuButton>
            </SidebarMenuItem>
            <SidebarMenuItem>
              <SidebarMenuButton>Projects</SidebarMenuButton>
            </SidebarMenuItem>
            <SidebarMenuItem>
              <SidebarMenuButton>Tasks</SidebarMenuButton>
            </SidebarMenuItem>
            <SidebarMenuItem>
              <SidebarMenuButton>Calendar</SidebarMenuButton>
            </SidebarMenuItem>
            <SidebarMenuItem>
              <SidebarMenuButton>Reports</SidebarMenuButton>
            </SidebarMenuItem>
          </SidebarMenu>
        </SidebarContent>
        <SidebarFooter>
          <div style={{ padding: '12px', borderTop: '1px solid #e5e7eb', fontSize: '12px', color: '#6b7280' }}>
            <div>Version 1.2.3</div>
            <div>© 2024 My App</div>
          </div>
        </SidebarFooter>
      </Sidebar>
      
      <SidebarMainContent>
        <div className="p-6">
          <h1>Dashboard</h1>
          <p>This sidebar includes a footer with additional information.</p>
        </div>
      </SidebarMainContent>
    </div>
  );
}`}
/>

## Responsive Sidebar

<SidebarCodePreview
  title="Responsive Sidebar"
  preview={<SidebarResponsiveExample />}
  code={`import { useState, useEffect } from 'react';
import { 
  Sidebar, 
  SidebarHeader, 
  SidebarContent, 
  SidebarBrand,
  SidebarMenu,
  SidebarMenuItem,
  SidebarMenuButton,
  SidebarMainContent
} from '@azodik/ui';

function ResponsiveSidebarExample() {
  const [isCollapsed, setIsCollapsed] = useState(false);
  const [isMobile, setIsMobile] = useState(false);

  useEffect(() => {
    const checkScreenSize = () => {
      setIsMobile(window.innerWidth <= 768);
    };
    
    checkScreenSize();
    window.addEventListener('resize', checkScreenSize);
    return () => window.removeEventListener('resize', checkScreenSize);
  }, []);

  return (
    <div style={{ display: 'flex', height: '100vh' }}>
      <Sidebar 
        width={isMobile ? 280 : isCollapsed ? 60 : 250}
        collapsed={!isMobile && isCollapsed}
        className={isMobile ? 'mobile-sidebar' : ''}
      >
        <SidebarHeader>
          <SidebarBrand>Responsive App</SidebarBrand>
        </SidebarHeader>
        <SidebarContent>
          <SidebarMenu>
            <SidebarMenuItem>
              <SidebarMenuButton 
                active
                icon={<HomeIcon size={16} />}
              >
                Home
              </SidebarMenuButton>
            </SidebarMenuItem>
            <SidebarMenuItem>
              <SidebarMenuButton 
                icon={<BarChartIcon size={16} />}
              >
                Analytics
              </SidebarMenuButton>
            </SidebarMenuItem>
            <SidebarMenuItem>
              <SidebarMenuButton 
                icon={<MailIcon size={16} />}
              >
                Messages
              </SidebarMenuButton>
            </SidebarMenuItem>
            <SidebarMenuItem>
              <SidebarMenuButton 
                icon={<SettingsIcon size={16} />}
              >
                Settings
              </SidebarMenuButton>
            </SidebarMenuItem>
          </SidebarMenu>
        </SidebarContent>
      </Sidebar>
      
      <SidebarMainContent
        onSidebarToggle={handleSidebarToggle}
        isSidebarCollapsed={isSmallScreen ? false : isCollapsed}
        showToggleOnDesktop={true}
        hideToggleOnDesktop={false}
      >
        <div className="p-6">
          <h1>Responsive Layout</h1>
          <p>This sidebar adapts to different screen sizes.</p>
          <p>On mobile: Full-width overlay</p>
          <p>On desktop: Collapsible sidebar</p>
        </div>
      </SidebarMainContent>
    </div>
  );
}`}
/>

## Props

<PropsTable props={[
  {
    name: 'children',
    type: 'React.ReactNode',
    required: true,
    default: '-',
    description: 'Sidebar content (typically SidebarHeader, SidebarContent, SidebarFooter)'
  },
  {
    name: 'width',
    type: 'number',
    required: false,
    default: '250',
    description: 'Width of the sidebar in pixels'
  },
  {
    name: 'collapsed',
    type: 'boolean',
    required: false,
    default: 'false',
    description: 'Whether the sidebar is collapsed'
  },
  {
    name: 'showHeader',
    type: 'boolean',
    required: false,
    default: 'true',
    description: 'Whether to show the sidebar header'
  },
  {
    name: 'showFooter',
    type: 'boolean',
    required: false,
    default: 'true',
    description: 'Whether to show the sidebar footer'
  },
  {
    name: 'showBreadcrumb',
    type: 'boolean',
    required: false,
    default: 'true',
    description: 'Whether to show breadcrumb in main content'
  },
  {
    name: 'color',
    type: 'string',
    required: false,
    default: '-',
    description: 'Custom background color for the sidebar'
  },
  {
    name: 'userProfile',
    type: '{ name: string; email: string; avatar?: string }',
    required: false,
    default: '-',
    description: 'User profile information to display'
  },
  {
    name: 'onUserAction',
    type: '(action: string) => void',
    required: false,
    default: '-',
    description: 'Function called when user actions are triggered'
  },
  {
    name: 'className',
    type: 'string',
    required: false,
    default: '""',
    description: 'Additional CSS classes to apply'
  }
]} />

## SidebarItem Props

<PropsTable props={[
  {
    name: 'children',
    type: 'React.ReactNode',
    required: true,
    default: '-',
    description: 'Content of the sidebar item'
  },
  {
    name: 'icon',
    type: 'React.ReactNode',
    required: false,
    default: '-',
    description: 'Icon to display with the sidebar item'
  },
  {
    name: 'active',
    type: 'boolean',
    required: false,
    default: 'false',
    description: 'Whether this item is currently active'
  },
  {
    name: 'badge',
    type: 'string | number',
    required: false,
    default: '-',
    description: 'Badge to display on the sidebar item'
  },
  {
    name: 'tooltip',
    type: 'string',
    required: false,
    default: '-',
    description: 'Tooltip text for the sidebar item'
  },
  {
    name: 'show',
    type: 'boolean',
    required: false,
    default: 'true',
    description: 'Whether to show the sidebar item'
  },
  {
    name: 'className',
    type: 'string',
    required: false,
    default: '""',
    description: 'Additional CSS classes to apply'
  }
]} />

## UserProfile Interface

<PropsTable props={[
  {
    name: 'name',
    type: 'string',
    required: true,
    default: '-',
    description: 'User display name'
  },
  {
    name: 'email',
    type: 'string',
    required: true,
    default: '-',
    description: 'User email address'
  },
  {
    name: 'avatar',
    type: 'string',
    required: false,
    default: '-',
    description: 'URL of user avatar image'
  }
]} />

## SidebarHeader Props

<PropsTable props={[
  {
    name: 'children',
    type: 'React.ReactNode',
    required: true,
    default: '-',
    description: 'Header content'
  },
  {
    name: 'show',
    type: 'boolean',
    required: false,
    default: 'true',
    description: 'Whether to show the header'
  },
  {
    name: 'className',
    type: 'string',
    required: false,
    default: '""',
    description: 'Additional CSS classes to apply'
  }
]} />

## SidebarContent Props

<PropsTable props={[
  {
    name: 'children',
    type: 'React.ReactNode',
    required: true,
    default: '-',
    description: 'Content area children'
  },
  {
    name: 'className',
    type: 'string',
    required: false,
    default: '""',
    description: 'Additional CSS classes to apply'
  }
]} />

## SidebarFooter Props

<PropsTable props={[
  {
    name: 'children',
    type: 'React.ReactNode',
    required: true,
    default: '-',
    description: 'Footer content'
  },
  {
    name: 'show',
    type: 'boolean',
    required: false,
    default: 'true',
    description: 'Whether to show the footer'
  },
  {
    name: 'className',
    type: 'string',
    required: false,
    default: '""',
    description: 'Additional CSS classes to apply'
  }
]} />

## SidebarMainContent Props

<PropsTable props={[
  {
    name: 'children',
    type: 'React.ReactNode',
    required: true,
    default: '-',
    description: 'Main content area children'
  },
  {
    name: 'breadcrumbItems',
    type: 'Array<{ label: string; href?: string; current?: boolean }>',
    required: false,
    default: '-',
    description: 'Breadcrumb navigation items'
  },
  {
    name: 'breadcrumb',
    type: 'React.ReactNode',
    required: false,
    default: '-',
    description: 'Custom breadcrumb component'
  },
  {
    name: 'onSidebarToggle',
    type: '() => void',
    required: false,
    default: '-',
    description: 'Function called when sidebar toggle is clicked'
  },
  {
    name: 'isSidebarCollapsed',
    type: 'boolean',
    required: false,
    default: 'false',
    description: 'Whether the sidebar is currently collapsed'
  },
  {
    name: 'sidebarToggleIcon',
    type: 'React.ReactNode',
    required: false,
    default: '-',
    description: 'Custom icon for sidebar toggle button'
  },
  {
    name: 'showBreadcrumb',
    type: 'boolean',
    required: false,
    default: 'true',
    description: 'Whether to show breadcrumb navigation'
  },
  {
    name: 'showToggleButton',
    type: 'boolean',
    required: false,
    default: 'true',
    description: 'Whether to show the sidebar toggle button'
  },
  {
    name: 'hideToggleOnDesktop',
    type: 'boolean',
    required: false,
    default: 'false',
    description: 'Whether to hide toggle button on desktop screens'
  },
  {
    name: 'className',
    type: 'string',
    required: false,
    default: '""',
    description: 'Additional CSS classes to apply'
  }
]} />

## Accessibility

- Sidebar has proper semantic structure
- Screen readers can navigate menu items
- Keyboard navigation is supported
- Focus management is handled correctly
- Proper ARIA attributes are applied

## Design Guidelines

- Use clear and descriptive navigation labels
- Keep the number of top-level items reasonable
- Provide clear visual feedback for active states
- Use consistent icons and styling
- Consider mobile responsiveness
