import { Card, CardContent, CardHeader, CardTitle, Button } from '@azodik/ui';
import { PropsTable, CodeBlock, CodeBlockWithPreview } from '@/components/docs';
import { useLanguageTranslation } from '@/hooks/useLanguageTranslation';

export const CardComponentHeader = () => {
  const { t } = useLanguageTranslation();
  return <h1>{t('cardComponent')}</h1>;
};

export const CardDescription = () => {
  const { t } = useLanguageTranslation();
  return <p>{t('cardDescription')}</p>;
};

export const CardIntroduction = () => {
  const { t } = useLanguageTranslation();
  return (
    <>
      <h2>{t('introduction')}</h2>
      <p>{t('cardIntroDesc')}</p>
      <ul>
        <li>{t('cardMainWrapper')}</li>
        <li>{t('cardHeader')}</li>
        <li>{t('cardContent')}</li>
        <li>{t('cardCustomization')}</li>
        <li>{t('cardInteractions')}</li>
        <li>{t('cardAccessibility')}</li>
      </ul>
    </>
  );
};

export const WhenToUseSection = () => {
  const { t } = useLanguageTranslation();
  return (
    <>
      <h3>{t('whenToUse')}</h3>
      <ul>
        <li>{t('displayRelatedInformationTogether')}</li>
        <li>{t('createContentBlocksOrSections')}</li>
        <li>{t('showProductInformationOrUserProfiles')}</li>
        <li>{t('groupFormElementsOrSettings')}</li>
        <li>{t('displayStatisticsOrMetrics')}</li>
      </ul>
    </>
  );
};

export const UsageSection = () => {
  const { t } = useLanguageTranslation();
  return <h2>{t('usage')}</h2>;
};

export const VariantsSection = () => {
  const { t } = useLanguageTranslation();
  return <h2>{t('variants')}</h2>;
};

export const SomeVariantsExample = () => {
  const { t } = useLanguageTranslation();
  return <h2>{t('someVariantsExample')}</h2>;
};

export const AllCardVariantsTitle = () => {
  const { t } = useLanguageTranslation();
  return t('allCardVariants');
};

export const WithHeaderAndTitleSection = () => {
  const { t } = useLanguageTranslation();
  return <h2>{t('withHeaderAndTitle')}</h2>;
};

export const CardWithHeaderTitle = () => {
  const { t } = useLanguageTranslation();
  return t('cardWithHeader');
};

export const WithActionsSection = () => {
  const { t } = useLanguageTranslation();
  return <h2>{t('withActions')}</h2>;
};

export const CardWithActionsTitle = () => {
  const { t } = useLanguageTranslation();
  return t('cardWithActions');
};

export const PropsSection = () => {
  const { t } = useLanguageTranslation();
  return <h2>{t('props')}</h2>;
};

export const AccessibilitySection = () => {
  const { t } = useLanguageTranslation();
  return <h2>{t('accessibility')}</h2>;
};

export const DesignGuidelinesSection = () => {
  const { t } = useLanguageTranslation();
  return <h2>{t('designGuidelines')}</h2>;
};

export const AccessibilityList = () => {
  const { t } = useLanguageTranslation();
  return (
    <ul>
      <li>{t('cardsHaveSemanticStructure')}</li>
      <li>{t('focusManagementForInteractiveCards')}</li>
      <li>{t('screenReadersNavigateCardContent')}</li>
      <li>{t('properHeadingHierarchyWhenUsingCardTitle')}</li>
    </ul>
  );
};

export const DesignGuidelinesList = () => {
  const { t } = useLanguageTranslation();
  return (
    <ul>
      <li>{t('useConsistentPaddingAndSpacing')}</li>
      <li>{t('groupRelatedContentTogether')}</li>
      <li>{t('provideClearVisualHierarchy')}</li>
      <li>{t('useAppropriateElevationForDepth')}</li>
      <li>{t('ensureSufficientContrast')}</li>
    </ul>
  );
};

<CardComponentHeader />

<CardDescription />

<CardIntroduction />

<WhenToUseSection />

<UsageSection />

<CodeBlock language="tsx">
{`import { Card } from '@azodik/ui';`}
</CodeBlock>

<VariantsSection />

<SomeVariantsExample />

<CodeBlockWithPreview 
  title={<AllCardVariantsTitle />}
  preview={
    <div className="space-y-4">
      <Card padding="sm" rounded="sm">
        <CardContent>Small padding card</CardContent>
      </Card>
      <Card padding="md" rounded="sm" className="mt-5">
        <CardContent>Medium padding card</CardContent>
      </Card>
      <Card padding="lg" rounded="sm" className="mt-5">
        <CardContent>Large padding card</CardContent>
      </Card>
    </div>
  }
>
{`import { Card, CardContent } from '@azodik/ui';

export const CardExamples = () => {
  return (
    <div className="space-y-4">
      <Card padding="sm">
        <CardContent>Small padding card</CardContent>
      </Card>
      <Card padding="md">
        <CardContent>Medium padding card</CardContent>
      </Card>
      <Card padding="lg">
        <CardContent>Large padding card</CardContent>
      </Card>
    </div>
  );
};`}
</CodeBlockWithPreview>

<WithHeaderAndTitleSection />

<CodeBlockWithPreview 
  title={<CardWithHeaderTitle />}
  preview={
    <Card>
      <CardHeader>
        <CardTitle>Card Title</CardTitle>
      </CardHeader>
      <CardContent>
        <p>This is the card content with a header.</p>
      </CardContent>
    </Card>
  }
>
{`import { Card, CardContent, CardHeader, CardTitle } from '@azodik/ui';

export const CardWithHeaderExample = () => {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Card Title</CardTitle>
      </CardHeader>
      <CardContent>
        <p>This is the card content with a header.</p>
      </CardContent>
    </Card>
  );
};`}
</CodeBlockWithPreview>

<WithActionsSection />

<CodeBlockWithPreview 
  title={<CardWithActionsTitle />}
  preview={
    <Card>
      <CardHeader>
        <CardTitle>Product Card</CardTitle>
      </CardHeader>
      <CardContent>
        <p>Product description goes here. test test test test test</p>
        <div className="flex gap-2 mt-4 justify-center items-center">
          <Button variant="primary" size="sm" width="100px">Buy Now</Button>
          <Button variant="outline" size="sm" className="ml-5">Learn More</Button>
        </div>
      </CardContent>
    </Card>
  }
>
{`import { Card, CardContent, CardHeader, CardTitle, Button } from '@azodik/ui';

export const CardWithActionsExample = () => {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Product Card</CardTitle>
      </CardHeader>
      <CardContent>
        <p>Product description goes here.</p>
        <div className="flex gap-2 mt-4">
          <Button variant="primary">Buy Now</Button>
          <Button variant="outline">Learn More</Button>
        </div>
      </CardContent>
    </Card>
  );
};`}
</CodeBlockWithPreview>

<PropsSection />

<PropsTable props={[
  {
    name: 'children',
    type: 'React.ReactNode',
    required: true,
    default: '-',
    description: 'Content to display inside the card'
  },
  {
    name: 'variant',
    type: '"default" | "outlined" | "shadow"',
    required: false,
    default: '"default"',
    description: 'Visual style variant of the card'
  },
  {
    name: 'width',
    type: '"xs" | "sm" | "md" | "lg" | "xl" | string | number',
    required: false,
    default: '"fit-content"',
    description: 'Width of the card. Can be size preset, custom string, or number (pixels)'
  },
  {
    name: 'height',
    type: '"xs" | "sm" | "md" | "lg" | "xl" | string | number',
    required: false,
    default: '-',
    description: 'Height of the card. Can be size preset, custom string, or number (pixels)'
  },
  {
    name: 'padding',
    type: '"xs" | "sm" | "md" | "lg" | "xl" | string | number',
    required: false,
    default: '"md"',
    description: 'Padding size for the card. Can be size preset, custom string, or number (pixels)'
  },
  {
    name: 'rounded',
    type: '"xs" | "sm" | "md" | "lg" | "xl" | string | number',
    required: false,
    default: '"sm"',
    description: 'Border radius of the card. Can be size preset, custom string, or number (pixels)'
  },
  {
    name: 'backgroundColor',
    type: 'string',
    required: false,
    default: '-',
    description: 'Custom background color for the card'
  },
  {
    name: 'borderColor',
    type: 'string',
    required: false,
    default: '-',
    description: 'Custom border color for the card'
  },
  {
    name: 'shadow',
    type: 'boolean',
    required: false,
    default: 'true',
    description: 'Whether to show shadow on the card'
  },
  {
    name: 'hoverEffect',
    type: 'boolean',
    required: false,
    default: 'false',
    description: 'Whether to show hover effects on the card'
  },
  {
    name: 'className',
    type: 'string',
    required: false,
    default: '""',
    description: 'Additional CSS classes to apply'
  }
]} />

<AccessibilitySection />

<AccessibilityList />

<DesignGuidelinesSection />

<DesignGuidelinesList />

