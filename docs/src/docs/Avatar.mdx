import { Avatar, AvatarGroup, Card, CardContent, Flex, Center } from '@azodik/ui';
import { PropsTable, CodeBlock, CodeBlockWithPreview } from '@/components/docs';
import { useLanguageTranslation } from '@/hooks/useLanguageTranslation';

export const AvatarHeader = () => {
  const { t } = useLanguageTranslation();
  return <h1>{t('avatarComponent')}</h1>;
};

export const AvatarDescription = () => {
  const { t } = useLanguageTranslation();
  return <p>{t('avatarDescription')}</p>;
};

export const OverviewSection = () => {
  const { t } = useLanguageTranslation();
  return (
    <>
      <h2>{t('overview')}</h2>
      <p>{t('avatarOverviewDesc')}</p>
    </>
  );
};

export const WhenToUseSection = () => {
  const { t } = useLanguageTranslation();
  return (
    <>
      <h3>{t('whenToUse')}</h3>
      <ul>
        <li>{t('displayUserProfilePictures')}</li>
        <li>{t('showUserInitials')}</li>
        <li>{t('representUsersInLists')}</li>
        <li>{t('createConsistentUserRepresentation')}</li>
      </ul>
    </>
  );
};

export const UsageSection = () => {
  const { t } = useLanguageTranslation();
  return <h2>{t('usage')}</h2>;
};

export const VariantsSection = () => {
  const { t } = useLanguageTranslation();
  return <h2>{t('variants')}</h2>;
};

export const SomeVariantsExample = () => {
  const { t } = useLanguageTranslation();
  return <h2>{t('someVariantsExample')}</h2>;
};

export const AllAvatarVariantsTitle = () => {
  const { t } = useLanguageTranslation();
  return t('allAvatarVariants');
};

export const WithInitialsSection = () => {
  const { t } = useLanguageTranslation();
  return <h2>{t('withInitials')}</h2>;
};

export const AvatarWithInitialsTitle = () => {
  const { t } = useLanguageTranslation();
  return t('avatarWithInitials');
};

export const AvatarGroupSection = () => {
  const { t } = useLanguageTranslation();
  return <h2>{t('avatarGroup')}</h2>;
};

export const AvatarSizesTitle = () => {
  const { t } = useLanguageTranslation();
  return t('avatarSizes');
};

export const AvatarPropsSection = () => {
  const { t } = useLanguageTranslation();
  return <h2>{t('avatarProps')}</h2>;
};

export const AvatarGroupPropsSection = () => {
  const { t } = useLanguageTranslation();
  return <h2>{t('avatarGroupProps')}</h2>;
};

export const AccessibilitySection = () => {
  const { t } = useLanguageTranslation();
  return <h2>{t('accessibility')}</h2>;
};

export const DesignGuidelinesSection = () => {
  const { t } = useLanguageTranslation();
  return <h2>{t('designGuidelines')}</h2>;
};

export const AccessibilityList = () => {
  const { t } = useLanguageTranslation();
  return (
    <ul>
      <li>{t('avatarsHaveAltText')}</li>
      <li>{t('initialsGeneratedFromName')}</li>
      <li>{t('colorContrastMeetsWCAG')}</li>
      <li>{t('focusStatesProvided')}</li>
    </ul>
  );
};

export const DesignGuidelinesList = () => {
  const { t } = useLanguageTranslation();
  return (
    <ul>
      <li>{t('useConsistentSizing')}</li>
      <li>{t('provideMeaningfulAltText')}</li>
      <li>{t('useInitialsAsFallback')}</li>
      <li>{t('considerContextWhenChoosingSizes')}</li>
      <li>{t('ensureAvatarsRecognizable')}</li>
    </ul>
  );
};

<AvatarHeader />

<AvatarDescription />

<OverviewSection />

<WhenToUseSection />

<UsageSection />

<CodeBlock language="tsx">
{`import { Avatar } from '@azodik/ui';`}
</CodeBlock>

<VariantsSection />

<SomeVariantsExample />

<CodeBlockWithPreview 
  title={<AllAvatarVariantsTitle />}
  preview={
    <Center>
      <Flex align="center" gap="4" wrap="wrap">
        <Avatar name="Henna Morris" size="xs" initials="HM"/>
        <Avatar name="Shane John" size="sm" initials="SJ"/>
        <Avatar name="Pedro Pascal" size="md" initials="PP"/>
        <Avatar name="Alice Brown" size="lg" initials="AB" />
      </Flex>
    </Center>
  }
>
{`import { Avatar, Flex, Center } from '@azodik/ui';

export const AvatarExamples = () => {
  return (
    <Center>
      <Flex align="center" gap="4">
        <Avatar 
          src="/path/to/image.jpg"
          alt="John Doe"
          size="sm"
        />
        <Avatar name="Jane Smith" size="md" />
        <Avatar name="Bob Wilson" size="xs" />
      </Flex>
    </Center>
  );
};`}
</CodeBlockWithPreview>

<WithInitialsSection />

<CodeBlockWithPreview 
  title={<AvatarWithInitialsTitle />}
  preview={
    <Avatar 
      name="John Doe"
      size="md"
      initials="JD"
    />
  }
>
{`import { Avatar } from '@azodik/ui';

export const UserAvatarExample = () => {
  return (
      <Avatar 
      name="John Doe"
      size="md"
    />
  );
}`}
</CodeBlockWithPreview>

<AvatarGroupSection />

<CodeBlockWithPreview 
  title={<AvatarSizesTitle />}
  preview={
    <Center>
      <Flex align="center" gap="4">
        <AvatarGroup max={3}>
          <Avatar size="sm" initials="A" />
          <Avatar size="sm" initials="B" />
          <Avatar size="sm" initials="C" />
          <Avatar size="sm" initials="D" />
          <Avatar size="sm" initials="E" />
          <Avatar size="sm" initials="F" />
        </AvatarGroup>
      </Flex>
    </Center>
  }
>
{`import { Avatar, Flex, Center } from '@azodik/ui';

export const AvatarSizesExample = () => {
  return (
    <Center>
      <Flex align="center" gap="4">
        <Avatar name="John Doe" size="xs" />
        <Avatar name="Jane Smith" size="sm" />
        <Avatar name="Bob Wilson" size="md" />
        <Avatar name="Alice Brown" size="lg" />
      </Flex>
    </Center>
  );
}`}
</CodeBlockWithPreview>

<AvatarPropsSection />

<PropsTable props={[
  {
    name: 'src',
    type: 'string',
    required: false,
    default: '-',
    description: 'URL of the profile image'
  },
  {
    name: 'alt',
    type: 'string',
    required: false,
    default: '"Avatar"',
    description: 'Alt text for the image. Defaults to "Avatar" if not provided'
  },
  {
    name: 'children',
    type: 'React.ReactNode',
    required: false,
    default: '-',
    description: 'Content used to generate initials if no initials prop is provided'
  },
  {
    name: 'size',
    type: '"xs" | "sm" | "md" | "lg" | "xl"',
    required: false,
    default: '"lg"',
    description: 'Size of the avatar'
  },
  {
    name: 'status',
    type: '"online" | "away" | "offline" | "busy"',
    required: false,
    default: '-',
    description: 'Status indicator for the avatar'
  },
  {
    name: 'initials',
    type: 'string',
    required: false,
    default: 'Auto-generated from children',
    description: 'Custom initials to display. If not provided, generates from children'
  },
  {
    name: 'className',
    type: 'string',
    required: false,
    default: '""',
    description: 'Additional CSS classes to apply'
  }
]} />

<AvatarGroupPropsSection />

<PropsTable props={[
  {
    name: 'children',
    type: 'React.ReactNode',
    required: true,
    default: '-',
    description: 'Avatar components to display in the group'
  },
  {
    name: 'max',
    type: 'number',
    required: false,
    default: '3',
    description: 'Maximum number of avatars to display before showing "more" indicator'
  },
  {
    name: 'moreLabel',
    type: 'string',
    required: false,
    default: '"+{count}"',
    description: 'Label template for the "more" indicator. Use {count} placeholder for the number'
  },
  {
    name: 'className',
    type: 'string',
    required: false,
    default: '""',
    description: 'Additional CSS classes to apply to the group'
  }
]} />

<AccessibilitySection />

<AccessibilityList />

<DesignGuidelinesSection />

<DesignGuidelinesList />