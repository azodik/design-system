import { Table, TableHeader, TableBody, TableRow, TableHeaderCell, TableCell, Card, CardContent } from '@azodik/ui';
import { PropsTable, CodeBlock, CodeBlockWithPreview } from '../components/docs';
import { ComponentRenderer } from '../components/docs/ComponentRenderer';

# Table Component

The Table component provides a structured way to display data in rows and columns. It's perfect for presenting tabular data in a clean and organized format.

## Overview

Tables are essential for displaying structured data in a readable format. They help users compare information across different categories and find specific data points quickly.

### When to use
- Displaying structured data in rows and columns
- Showing comparison data
- Presenting lists with multiple attributes
- Displaying financial or statistical data
- Creating data grids

### When not to use
- For simple lists (use regular lists instead)
- For complex interactive data (use DataTable instead)
- For layout purposes (use CSS Grid or Flexbox)
- When data doesn't have a clear structure

## Usage

<CodeBlock language="tsx">
{`import { Table } from '@azodik/ui';`}
</CodeBlock>

## Variants

<ComponentRenderer componentName="table" />

## Some Variants Example

<CodeBlockWithPreview 
  title="All Table Variants"
  preview={
    <Table>
      <TableHeader>
        <TableRow>
          <TableHeaderCell>Name</TableHeaderCell>
          <TableHeaderCell>Email</TableHeaderCell>
          <TableHeaderCell>Role</TableHeaderCell>
        </TableRow>
      </TableHeader>
      <TableBody>
        <TableRow>
          <TableCell>John Doe</TableCell>
          <TableCell>john@example.com</TableCell>
          <TableCell>Admin</TableCell>
        </TableRow>
        <TableRow>
          <TableCell>Jane Smith</TableCell>
          <TableCell>jane@example.com</TableCell>
          <TableCell>User</TableCell>
        </TableRow>
      </TableBody>
    </Table>
  }
>
{`import { Table, TableHeader, TableBody, TableRow, TableHeaderCell, TableCell } from '@azodik/ui';

export const TableExamples = () => {
  const data = [
    { id: 1, name: 'John Doe', email: 'john@example.com', role: 'Admin' },
    { id: 2, name: 'Jane Smith', email: 'jane@example.com', role: 'User' },
    { id: 3, name: 'Bob Johnson', email: 'bob@example.com', role: 'User' }
  ];

  return (
    <Table>
      <TableHeader>
        <TableRow>
          <TableHeaderCell>Name</TableHeaderCell>
          <TableHeaderCell>Email</TableHeaderCell>
          <TableHeaderCell>Role</TableHeaderCell>
        </TableRow>
      </TableHeader>
      <TableBody>
        {data.map((user) => (
          <TableRow key={user.id}>
            <TableCell>{user.name}</TableCell>
            <TableCell>{user.email}</TableCell>
            <TableCell>{user.role}</TableCell>
          </TableRow>
        ))}
      </TableBody>
    </Table>
  );
};`}
</CodeBlockWithPreview>

## Basic Usage

<CodeBlockWithPreview 
  title="Basic Table"
  preview={
    <Table>
      <TableHeader>
        <TableRow>
          <TableHeaderCell>Name</TableHeaderCell>
          <TableHeaderCell>Email</TableHeaderCell>
          <TableHeaderCell>Role</TableHeaderCell>
        </TableRow>
      </TableHeader>
      <TableBody>
        <TableRow>
          <TableCell>John Doe</TableCell>
          <TableCell>john@example.com</TableCell>
          <TableCell>Admin</TableCell>
        </TableRow>
        <TableRow>
          <TableCell>Jane Smith</TableCell>
          <TableCell>jane@example.com</TableCell>
          <TableCell>User</TableCell>
        </TableRow>
      </TableBody>
    </Table>
  }
>
{`import { Table, TableHeader, TableBody, TableRow, TableHeaderCell, TableCell } from '@azodik/ui';

export const TableExample = () => {
  const data = [
    { id: 1, name: 'John Doe', email: 'john@example.com', role: 'Admin' },
    { id: 2, name: 'Jane Smith', email: 'jane@example.com', role: 'User' },
    { id: 3, name: 'Bob Johnson', email: 'bob@example.com', role: 'User' }
  ];

  return (
    <Table>
      <TableHeader>
        <TableRow>
          <TableHeaderCell>Name</TableHeaderCell>
          <TableHeaderCell>Email</TableHeaderCell>
          <TableHeaderCell>Role</TableHeaderCell>
        </TableRow>
      </TableHeader>
      <TableBody>
        {data.map((user) => (
          <TableRow key={user.id}>
            <TableCell>{user.name}</TableCell>
            <TableCell>{user.email}</TableCell>
            <TableCell>{user.role}</TableCell>
          </TableRow>
        ))}
      </TableBody>
    </Table>
  );
};`}
</CodeBlockWithPreview>

## With Actions

<CodeBlock title="Table with Actions">
{`import { Table, TableHeader, TableBody, TableRow, TableHeaderCell, TableCell, Button } from '@azodik/ui';

export const TableWithActionsExample = () => {
  const data = [
    { id: 1, name: 'John Doe', email: 'john@example.com', role: 'Admin' },
    { id: 2, name: 'Jane Smith', email: 'jane@example.com', role: 'User' }
  ];

  const handleEdit = (id) => {
    console.log('Edit user:', id);
  };

  const handleDelete = (id) => {
    console.log('Delete user:', id);
  };

  return (
    <Table>
      <TableHeader>
        <TableRow>
          <TableHeaderCell>Name</TableHeaderCell>
          <TableHeaderCell>Email</TableHeaderCell>
          <TableHeaderCell>Role</TableHeaderCell>
          <TableHeaderCell>Actions</TableHeaderCell>
        </TableRow>
      </TableHeader>
      <TableBody>
        {data.map((user) => (
          <TableRow key={user.id}>
            <TableCell>{user.name}</TableCell>
            <TableCell>{user.email}</TableCell>
            <TableCell>{user.role}</TableCell>
            <TableCell>
              <div className="flex gap-2">
                <Button size="sm" variant="outline" onClick={() => handleEdit(user.id)}>
                  Edit
                </Button>
                <Button size="sm" variant="destructive" onClick={() => handleDelete(user.id)}>
                  Delete
                </Button>
              </div>
            </TableCell>
          </TableRow>
        ))}
      </TableBody>
    </Table>
  );
}`}
</CodeBlock>

## With Sorting

<CodeBlock title="Table with Sorting">
{`import { Table, TableHeader, TableBody, TableRow, TableHeaderCell, TableCell } from '@azodik/ui';
import { useState } from 'react';

export const SortableTableExample = () => {
  const [sortField, setSortField] = useState('name');
  const [sortDirection, setSortDirection] = useState('asc');

  const data = [
    { id: 1, name: 'John Doe', age: 30, department: 'Engineering' },
    { id: 2, name: 'Jane Smith', age: 25, department: 'Design' },
    { id: 3, name: 'Bob Johnson', age: 35, department: 'Marketing' }
  ];

  const handleSort = (field) => {
    if (sortField === field) {
      setSortDirection(sortDirection === 'asc' ? 'desc' : 'asc');
    } else {
      setSortField(field);
      setSortDirection('asc');
    }
  };

  const sortedData = [...data].sort((a, b) => {
    const aValue = a[sortField];
    const bValue = b[sortField];
    
    if (sortDirection === 'asc') {
      return aValue > bValue ? 1 : -1;
    } else {
      return aValue < bValue ? 1 : -1;
    }
  });

  return (
    <Table>
      <TableHeader>
        <TableRow>
          <TableHeaderCell 
            className="cursor-pointer hover:bg-gray-50"
            onClick={() => handleSort('name')}
          >
            Name {sortField === 'name' && (sortDirection === 'asc' ? '↑' : '↓')}
          </TableHeaderCell>
          <TableHeaderCell 
            className="cursor-pointer hover:bg-gray-50"
            onClick={() => handleSort('age')}
          >
            Age {sortField === 'age' && (sortDirection === 'asc' ? '↑' : '↓')}
          </TableHeaderCell>
          <TableHeaderCell 
            className="cursor-pointer hover:bg-gray-50"
            onClick={() => handleSort('department')}
          >
            Department {sortField === 'department' && (sortDirection === 'asc' ? '↑' : '↓')}
          </TableHeaderCell>
        </TableRow>
      </TableHeader>
      <TableBody>
        {sortedData.map((user) => (
          <TableRow key={user.id}>
            <TableCell>{user.name}</TableCell>
            <TableCell>{user.age}</TableCell>
            <TableCell>{user.department}</TableCell>
          </TableRow>
        ))}
      </TableBody>
    </Table>
  );
}`}
</CodeBlock>

## Props

<PropsTable props={[
  {
    name: 'children',
    type: 'React.ReactNode',
    required: false,
    default: '-',
    description: 'Table content (TableHeader, TableBody, etc.)'
  },
  {
    name: 'className',
    type: 'string',
    required: false,
    default: '-',
    description: 'Additional CSS classes to apply'
  }
]} />

## Accessibility

- Tables have proper semantic structure
- Screen readers can navigate table content
- Column headers are properly associated
- Keyboard navigation is supported
- Table captions can be added for context

## Design Guidelines

- Use clear and descriptive column headers
- Keep tables scannable with proper spacing
- Use consistent alignment for data types
- Consider responsive design for mobile
- Use alternating row colors for better readability
