import { Tabs, TabList, TabTrigger, TabContent, Card, CardContent } from '@azodik/ui';
import { PropsTable, CodeBlock, CodeBlockWithPreview } from '../components/docs';
import { ComponentRenderer } from '../components/docs/ComponentRenderer';
import ComponentNavigation from '../components/docs/ComponentNavigation';

# Tabs Component

The Tabs component organizes content into multiple panels with tab navigation. It provides a clean way to group related content and improve user experience.

## Overview

Tabs help organize content into logical sections, making it easier for users to find and navigate through different types of information without leaving the current page.

### When to use
- Organizing related content into sections
- Switching between different views or modes
- Grouping form fields or settings
- Displaying different data sets
- Creating wizard-like interfaces

## Usage

<CodeBlock language="tsx">
{`import { Tabs, TabList, TabTrigger, TabContent } from '@azodik/ui';`}
</CodeBlock>

## Variants

<ComponentRenderer componentName="tabs" />

## Some Variants Example

<CodeBlockWithPreview 
  title="All Tabs Variants"
  preview={
    <Tabs defaultValue="tab1">
      <TabList>
        <TabTrigger value="tab1" borderWidth={4} width="100px" borderColor="#f97316">Tab 1</TabTrigger>
        <TabTrigger value="tab2" borderWidth={2} width="100px" borderColor="#3b82f6">Tab 2</TabTrigger>
        <TabTrigger value="tab3" borderWidth={5} width="100px" borderColor="#10b981">Tab 3</TabTrigger>
      </TabList>
      
      <TabContent value="tab1">
        <Card className="p-6">
          <h3 className="text-lg font-semibold mb-2">Tab 1 Content</h3>
          <p className="text-gray-600">This is the content for tab 1.</p>
        </Card>
      </TabContent>
      
      <TabContent value="tab2">
        <Card className="p-6">
          <h3 className="text-lg font-semibold mb-2">Tab 2 Content</h3>
          <p className="text-gray-600">This is the content for tab 2.</p>
        </Card>
      </TabContent>
      
      <TabContent value="tab3">
        <Card className="p-6">
          <h3 className="text-lg font-semibold mb-2">Tab 3 Content</h3>
          <p className="text-gray-600">This is the content for tab 3.</p>
        </Card>
      </TabContent>
    </Tabs>
  }
>
{`import React, { useState } from "react";
import { Tabs, TabList, TabTrigger, TabContent, Card } from "@azodik/ui";

export const TabsExample = () => {
  const [activeTab, setActiveTab] = useState("tab1");

  return (
    <Tabs value={activeTab} onValueChange={setActiveTab}>
      <TabList>
        <TabTrigger value="tab1" borderWidth={4} width="100px" borderColor="#f97316">Tab 1</TabTrigger>
        <TabTrigger value="tab2" borderWidth={2} width="100px" borderColor="#3b82f6">Tab 2</TabTrigger>
        <TabTrigger value="tab3" borderWidth={5} width="100px" borderColor="#10b981">Tab 3</TabTrigger>
      </TabList>
      
      <TabContent value="tab1">
        <Card className="p-6">
          <h3 className="text-lg font-semibold mb-2">Tab 1 Content</h3>
          <p className="text-gray-600">This is the content for tab 1.</p>
        </Card>
      </TabContent>
      
      <TabContent value="tab2">
        <Card className="p-6">
          <h3 className="text-lg font-semibold mb-2">Tab 2 Content</h3>
          <p className="text-gray-600">This is the content for tab 2.</p>
        </Card>
      </TabContent>
      
      <TabContent value="tab3">
        <Card className="p-6">
          <h3 className="text-lg font-semibold mb-2">Tab 3 Content</h3>
          <p className="text-gray-600">This is the content for tab 3.</p>
        </Card>
      </TabContent>
    </Tabs>
  );
};`}
</CodeBlockWithPreview>

## Props

<PropsTable props={[
  {
    name: 'children',
    type: 'React.ReactNode',
    required: true,
    default: '-',
    description: 'Tab content (typically TabList and TabContent components)'
  },
  {
    name: 'defaultValue',
    type: 'string',
    required: false,
    default: '-',
    description: 'Default active tab value (for uncontrolled mode)'
  },
  {
    name: 'value',
    type: 'string',
    required: false,
    default: '-',
    description: 'Currently active tab value (for controlled mode)'
  },
  {
    name: 'onValueChange',
    type: '(value: string) => void',
    required: false,
    default: '-',
    description: 'Function called when tab changes'
  },
  {
    name: 'className',
    type: 'string',
    required: false,
    default: '""',
    description: 'Additional CSS classes to apply'
  }
]} />

## TabList Props

<PropsTable props={[
  {
    name: 'children',
    type: 'React.ReactNode',
    required: true,
    default: '-',
    description: 'Tab trigger elements (typically TabTrigger components)'
  },
  {
    name: 'activeValue',
    type: 'string',
    required: false,
    default: '-',
    description: 'Currently active tab value (passed from parent Tabs)'
  },
  {
    name: 'onValueChange',
    type: '(value: string) => void',
    required: false,
    default: '-',
    description: 'Function called when tab changes (passed from parent Tabs)'
  },
  {
    name: 'className',
    type: 'string',
    required: false,
    default: '""',
    description: 'Additional CSS classes to apply'
  }
]} />

## TabTrigger Props

<PropsTable props={[
  {
    name: 'children',
    type: 'React.ReactNode',
    required: true,
    default: '-',
    description: 'Tab trigger content'
  },
  {
    name: 'value',
    type: 'string',
    required: true,
    default: '-',
    description: 'Unique value for this tab trigger'
  },
  {
    name: 'active',
    type: 'boolean',
    required: false,
    default: 'false',
    description: 'Whether this tab is currently active (managed by parent)'
  },
  {
    name: 'borderWidth',
    type: 'number',
    required: false,
    default: '3',
    description: 'Width of the active tab border in pixels'
  },
  {
    name: 'borderColor',
    type: 'string',
    required: false,
    default: '-',
    description: 'Color of the active tab border (defaults to primary color)'
  },
  {
    name: 'width',
    type: 'string | number',
    required: false,
    default: '-',
    description: 'Width of the tab trigger'
  },
  {
    name: 'className',
    type: 'string',
    required: false,
    default: '""',
    description: 'Additional CSS classes to apply'
  }
]} />

## TabContent Props

<PropsTable props={[
  {
    name: 'children',
    type: 'React.ReactNode',
    required: true,
    default: '-',
    description: 'Content to display when this tab is active'
  },
  {
    name: 'value',
    type: 'string',
    required: true,
    default: '-',
    description: 'Unique value for this tab content (must match TabTrigger value)'
  },
  {
    name: 'activeValue',
    type: 'string',
    required: false,
    default: '-',
    description: 'Currently active tab value (passed from parent Tabs)'
  },
  {
    name: 'onValueChange',
    type: '(value: string) => void',
    required: false,
    default: '-',
    description: 'Function called when tab changes (passed from parent Tabs)'
  },
  {
    name: 'className',
    type: 'string',
    required: false,
    default: '""',
    description: 'Additional CSS classes to apply'
  }
]} />

## Accessibility

- Tabs have proper ARIA attributes
- Keyboard navigation is supported
- Screen readers can identify active tab
- Focus management is handled correctly
- Tab content is properly associated

## Design Guidelines

- Use clear and descriptive tab labels
- Keep the number of tabs reasonable (3-5 tabs max)
- Ensure tab content is related and meaningful
- Use consistent styling across all tabs
- Consider mobile responsiveness for tab navigation

<ComponentNavigation />
