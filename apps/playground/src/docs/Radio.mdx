import { Radio, Card, CardContent } from '@azodik/ui';
import { PropsTable, CodeBlock, CodeBlockWithPreview } from '../components/docs';
import { ComponentRenderer } from '../components/docs/ComponentRenderer';
import { useState } from 'react';

export const ControlledRadioExample = () => {
  const [selectedLanguage, setSelectedLanguage] = useState('');

  const handleChange = (e) => {
    setSelectedLanguage(e.target.value);
  };

  return (
    <div className="flex-vertical">
      <h3>Choose your preferred language:</h3>
      <Radio 
        id="english" 
        name="language" 
        value="english"
        label="English"
        checked={selectedLanguage === 'english'}
        onChange={handleChange}
      />
      <Radio 
        id="spanish" 
        name="language" 
        value="spanish"
        label="Spanish"
        checked={selectedLanguage === 'spanish'}
        onChange={handleChange}
      />
      <Radio 
        id="french" 
        name="language" 
        value="french"
        label="French"
        checked={selectedLanguage === 'french'}
        onChange={handleChange}
      />
      <p className="text-sm text-gray-500">
        Selected: {selectedLanguage || 'None'}
      </p>
    </div>
  );
};

export const DefaultRadioExample = () => {
  const [selectedSize, setSelectedSize] = useState('medium');

  const handleChange = (e) => {
    setSelectedSize(e.target.value);
  };

  return (
    <div className="flex-vertical">
      <h3>Choose your size:</h3>
      <Radio 
        id="small" 
        name="size" 
        value="small"
        label="Small"
        checked={selectedSize === 'small'}
        onChange={handleChange}
      />
      <Radio 
        id="medium" 
        name="size" 
        value="medium"
        label="Medium"
        checked={selectedSize === 'medium'}
        onChange={handleChange}
      />
      <Radio 
        id="large" 
        name="size" 
        value="large"
        label="Large"
        checked={selectedSize === 'large'}
        onChange={handleChange}
      />
    </div>
  );
};

export const ValidatedRadioExample = () => {
  const [selectedOption, setSelectedOption] = useState('');
  const [error, setError] = useState('');

  const handleChange = (e) => {
    setSelectedOption(e.target.value);
    if (e.target.value) {
      setError('');
    }
  };

  const handleBlur = () => {
    if (!selectedOption) {
      setError('Please select an option');
    }
  };

  return (
    <div className="flex-vertical">
      <h3>Choose your payment method: *</h3>
      <div className="flex items-center gap-sm">
        <Radio 
          id="credit" 
          name="payment" 
          value="credit"
          checked={selectedOption === 'credit'}
          onChange={handleChange}
          onBlur={handleBlur}
        />
        <label htmlFor="credit">Credit Card</label>
      </div>
      <div className="flex items-center gap-sm">
        <Radio 
          id="debit" 
          name="payment" 
          value="debit"
          checked={selectedOption === 'debit'}
          onChange={handleChange}
          onBlur={handleBlur}
        />
        <label htmlFor="debit">Debit Card</label>
      </div>
      <div className="flex items-center gap-sm">
        <Radio 
          id="paypal" 
          name="payment" 
          value="paypal"
          checked={selectedOption === 'paypal'}
          onChange={handleChange}
          onBlur={handleBlur}
        />
        <label htmlFor="paypal">PayPal</label>
      </div>
      {error && <p className="text-sm text-red-500">{error}</p>}
    </div>
  );
};

export const SizedRadioExample = () => {
  return (
    <div className="flex-vertical">
      <div>
        <h4>Small Radio</h4>
        <div className="flex items-center gap-sm">
          <Radio id="small1" name="small" value="1" size="sm" />
          <label htmlFor="small1">Option 1</label>
        </div>
        <div className="flex items-center gap-sm">
          <Radio id="small2" name="small" value="2" size="sm" />
          <label htmlFor="small2">Option 2</label>
        </div>
      </div>
      
      <div>
        <h4>Medium Radio</h4>
        <div className="flex items-center gap-sm">
          <Radio id="medium1" name="medium" value="1" size="md" />
          <label htmlFor="medium1">Option 1</label>
        </div>
        <div className="flex items-center gap-sm">
          <Radio id="medium2" name="medium" value="2" size="md" />
          <label htmlFor="medium2">Option 2</label>
        </div>
      </div>
      
      <div>
        <h4>Large Radio</h4>
        <div className="flex items-center gap-sm">
          <Radio id="large1" name="large" value="1" size="lg" />
          <label htmlFor="large1">Option 1</label>
        </div>
        <div className="flex items-center gap-sm">
          <Radio id="large2" name="large" value="2" size="lg" />
          <label htmlFor="large2">Option 2</label>
        </div>
      </div>
    </div>
  );
};

# Radio Component

The Radio component provides single-choice controls for selecting one option from a group. It's perfect for forms that require exclusive selection.

## Introduction

The Radio component includes several complementary features to handle various single-choice selection scenarios:

- Radio: the main wrapper for creating single-choice selection controls with mutually exclusive options.
- Radio Groups: automatic grouping functionality that ensures only one option can be selected at a time.
- Radio States: multiple interaction states (checked, unchecked, disabled) with clear visual feedback.
- Radio Validation: built-in validation support with error messages and form integration capabilities.
- Radio Accessibility: proper ARIA attributes, keyboard navigation, and screen reader support for form controls.
- Radio Customization: flexible sizing, styling, and labeling options for different design contexts.

### When to use
- Single selection from multiple options
- Choosing one option from a group
- Settings with mutually exclusive choices
- Form fields with limited, exclusive options
- Survey questions with single answers

## Usage

<CodeBlock language="tsx">
{`import { Radio } from '@azodik/ui';`}
</CodeBlock>

## Variants

<ComponentRenderer componentName="radio" />

## With Default Selection

<CodeBlockWithPreview 
  title="Radio with Default Selection"
  preview={<DefaultRadioExample />}
>
{`import { Radio } from '@azodik/ui';
import { useState } from 'react';

export const DefaultRadioGroupExample = () => {
  const [selectedSize, setSelectedSize] = useState('medium');

  const handleChange = (e) => {
    setSelectedSize(e.target.value);
  };

  return (
    <div className="flex-vertical">
      <h3>Choose your size:</h3>
      <div className="flex items-center gap-sm">
        <Radio 
          id="small" 
          name="size" 
          value="small"
          checked={selectedSize === 'small'}
          onChange={handleChange}
        />
        <label htmlFor="small">Small</label>
      </div>
      <div className="flex items-center gap-sm">
        <Radio 
          id="medium" 
          name="size" 
          value="medium"
          checked={selectedSize === 'medium'}
          onChange={handleChange}
        />
        <label htmlFor="medium">Medium</label>
      </div>
      <div className="flex items-center gap-sm">
        <Radio 
          id="large" 
          name="size" 
          value="large"
          checked={selectedSize === 'large'}
          onChange={handleChange}
        />
        <label htmlFor="large">Large</label>
      </div>
    </div>
  );
}`}
</CodeBlockWithPreview>

## Props

<PropsTable props={[
  {
    name: 'label',
    type: 'string',
    required: false,
    default: '-',
    description: 'Label text for the radio button'
  },
  {
    name: 'help',
    type: 'string',
    required: false,
    default: '-',
    description: 'Help text to display below the radio button'
  },
  {
    name: 'error',
    type: 'string',
    required: false,
    default: '-',
    description: 'Error message to display below the radio button'
  },
  {
    name: 'className',
    type: 'string',
    required: false,
    default: '""',
    description: 'Additional CSS classes to apply'
  }
]} />

## Accessibility

- Radio buttons have proper labels and associations
- Screen readers can identify radio button states
- Keyboard navigation is supported
- Focus management is handled correctly
- Proper ARIA attributes are applied

## Design Guidelines

- Always provide clear labels
- Use consistent sizing across your application
- Group related radio buttons together
- Provide clear visual feedback for selection
- Ensure sufficient touch targets on mobile
