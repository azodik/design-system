# Enable workspace protocol for pnpm
# This allows packages to reference each other using workspace:*
# When published, pnpm will replace workspace:* with actual versions
auto-install-peers=true
strict-peer-dependencies=false

