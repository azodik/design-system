# Enable workspace protocol for pnpm
# This allows packages to reference each other using workspace:*
# When published, pnpm will replace workspace:* with actual versions
# Note: pnpm-specific config options (auto-install-peers, strict-peer-dependencies)
# are set via pnpm config to avoid npm warnings when npm reads this file

# NPM Authentication
# The auth token is loaded from .npmrc.local (gitignored) or NPM_TOKEN environment variable
# To set up: Run 'pnpm run setup:auth' or create .npmrc.local manually
# Format for .npmrc.local: //registry.npmjs.org/:_authToken=YOUR_TOKEN_HERE
# Note: The token is injected via scripts/run-with-npm-token.sh, not directly in this file
@azodik:registry=https://registry.npmjs.org/
//registry.npmjs.org/:always-auth=true

